let num= prompt("dame un numero")

if (Number(num)%2==0 || Number(num)%3==0|| Number(num)%5==0|| Number(num)%7==0) {
    document.write("es divisible")
    
}else{
    document.write("no es divisible")

}